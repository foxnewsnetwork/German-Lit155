<%= form_for @search do |s| %>
	<%= render 'shared/error_messages', :object => s.object %>
	<div class="content">
		<%= s.label :content, "Search: " %>
		<%= s.text_field :content %>
	</div>
	<%= r.submit %>
<% end %>
